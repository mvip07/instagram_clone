import React from 'react';
import styled from 'styled-components';

const Home = () => {
    return (
        <Wrapper>
            <h2>Home</h2>
        </Wrapper>
    );
}

export default Home;

const Wrapper = styled.div`
    
`
